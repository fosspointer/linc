// Command line arguments in linc!
#include `std.linc`

fn main(args: string[]) {
    index: mut u64
    for(in args argument) {
        puts('#' + @index + ": " + argument + '\n')
        ++index
    }
    {}
}