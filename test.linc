# Example program in linc!

{
    f32 degrees_fahrenheit = 98.6f32 # change this temperature
    f32 degrees_celsius = (degrees_fahrenheit - 32f32) * 5f32 / 9f32 # convert the temperature from fahrenheit to celsius
    
    string state = if degrees_celsius > 0f32 if degrees_celsius < 100f32 "liquid" else "gas" else "solid" # string corresponding to the water state at a given temperature

    puts("Water would be in " + state + " state in this temperature!") # print the state
    putc(10c) # newline
}